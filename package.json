{
  "scripts": {
    "init": "cd api && python -m venv venv && source venv/bin/activate || venv\\Scripts\\activate && pip install -r requirements.txt && cd .. && cd app && npm install",
    "api": "cd api && source venv/bin/activate || venv\\Scripts\\activate && fastapi dev main.py",
    "app": "cd app && npm run dev"
  }
}
